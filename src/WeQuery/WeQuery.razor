@namespace WeQ

<wequery id="@Id" data-role="query">
    @ChildContent
</wequery>

@code {

    public string Id { get; private set; }
    protected override void OnInitialized()
    {
        base.OnInitialized();
        Id = Guid.NewGuid().ToString().Replace("-", "");
    }
}
